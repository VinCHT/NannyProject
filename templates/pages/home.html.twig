{% extends 'base.html.twig' %}

{% block title 'Nourrices des Gones' %}
   {% block body %}
      <div class="jumbotron text-center">
         <h1>Nourrices des Gones</h1>
         <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. </p>
      </div>

      <div class="container">
         <h2>Les derniers inscrits</h2>
         <div class="row flex">
            {% for nanny in nannies %}
               <div class="col-3">
                  <div class="card">
                     <div class="card-body">
                        <h5 class="card-title">
                           <a href="{{ path('nanny.show', { id: nanny.id, slug: nanny.slug }) }}">{{ nanny.title}}</a>
                        </h5>
                        <p class="card-text">{{ nanny.city }} ({{ nanny.postalCode }})</p>
                        <div class="text-primary" style="font-weight: bold; font-size: 2rem;">{{ nanny.formattedPrice }} €</div>
                     </div>
                  </div>
               </div>
            {% endfor %}
         </div>
      </div>
   {% endblock %}
